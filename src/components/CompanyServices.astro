---
import { siteConfig } from "../config";
---

<section id="services" class="p-8 sm:p-12 md:p-16 lg:p-24">
  <div>
    <div class="grid grid-cols-1 lg:grid-cols-12 gap-8 lg:gap-16 items-start">
      <div class="lg:col-span-4">
        <h2
          class="text-3xl sm:text-4xl md:text-5xl xl:text-7xl font-bold text-gray-900"
        >
          Services
        </h2>
        <div
          class="w-[75px] h-[5px] mt-2 rounded-full"
          style={`background-color: ${siteConfig.accentColor}`}
        >
        </div>
      </div>

      <div class="lg:col-span-8 space-y-8">
        {
          siteConfig.companyServices?.length ? (
            <div class="space-y-6">
              {siteConfig.companyServices.map((service) => (
                <div class="space-y-2">
                  <h3 class="text-xl sm:text-2xl font-bold text-gray-900">
                    {service.name}
                  </h3>
                  {service.description ? (
                    <p class="text-base sm:text-lg md:text-xl leading-relaxed text-gray-600">
                      {service.description}
                    </p>
                  ) : null}
                </div>
              ))}
            </div>
          ) : (
            <p class="text-lg sm:text-xl md:text-2xl leading-relaxed text-gray-600">
              Add services in <span class="font-semibold text-gray-900">siteConfig.companyServices</span>.
            </p>
          )
        }
      </div>
    </div>
  </div>
</section>
